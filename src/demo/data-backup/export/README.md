# Code Generation

This directory is a placeholder. The actual IaC code is generated by the coding agent into `infra/bicep/` or `infra/tf/` inside your project's `infra/` directory.

Run `/generate-code-bicep` or `/generate-code-terraform` in your coding agent to generate infrastructure code.
