# Export Data

This directory contains sample data for the export section.

The export package is generated via the `/export-infrastructure --tool={bicep|terraform}` command in the coding agent. The actual export files would be created in an `infrastructure-plan/` directory at the project root.

This sample data structure shows what files would be included in the export package for both Bicep and Terraform options.
