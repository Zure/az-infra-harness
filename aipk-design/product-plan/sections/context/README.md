# Context

## Overview

See `product-plan/instructions/incremental/` for complete implementation instructions for this section.

## Components

See `components/` directory for all React components.

## Data Shapes

See `types.ts` for TypeScript interface definitions.

## Sample Data

See `sample-data.json` for test data showing the expected data structure.

## Testing

See `tests.md` for UI behavior test specifications.
